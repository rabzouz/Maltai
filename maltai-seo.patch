diff --git a/app.py b/app.py
index d0507cd..28d882d 100644
--- a/app.py
+++ b/app.py
@@ -153,6 +153,19 @@ def billing_success_page():
     return FileResponse(str(STATIC_DIR / "billing.html"))
 
 
+# Ajouté en dernier => middleware le plus externe : s'exécute avant l'auth.
+@app.middleware("http")
+async def canonical_host_redirect(request: Request, call_next):
+    """Redirige www.maltai.fr -> maltai.fr en 301 (évite le duplicate content)."""
+    host = request.headers.get("host", "")
+    if host.startswith("www."):
+        target = f"https://{host[4:]}{request.url.path}"
+        if request.url.query:
+            target += "?" + request.url.query
+        return RedirectResponse(target, status_code=301)
+    return await call_next(request)
+
+
 if __name__ == "__main__":
     import uvicorn
 
diff --git a/content/homepage_headings.md b/content/homepage_headings.md
new file mode 100644
index 0000000..52a5861
--- /dev/null
+++ b/content/homepage_headings.md
@@ -0,0 +1,29 @@
+# Structure des titres — homepage maltai.fr
+
+## H1 (un seul, descriptif)
+**Avant :** `MaltaiAI` (8 caractères — trop court)
+**Après :** `MaltaiAI — Le workspace IA open source et auto-hébergé pour agents, fichiers et automatisation`
+- Marque (`MaltaiAI`) rendue de façon imposante, proposition de valeur en sous-titre.
+- Un seul `<h1>` dans la page, cohérent avec le `<title>`.
+
+## Inventaire actuel
+- 1 × H1, 11 × H2, 18 × H3 (30 titres pour ~1125 mots).
+
+## Hiérarchie (H2 = blocs majeurs)
+- H2 Expérience → (intro)
+- H2 Comment ça marche → 6× H3 (Créer un compte … Gérer le workspace)
+- H2 Agent autonome → 7× H3 (Connecter … Plans, crédits et outils)
+- H2 Outils natifs → 6× H3 (Browser, Fichiers, Terminal, Brain, Ollama, Crédits)
+- H2 Cas d'usage
+- H2 Sécurité
+- H2 Accès / Plans
+- H2 FAQ
+- H2 Installer
+- H2 CTA final
+
+## Recommandations
+- La densité (30 titres) reste acceptable car chaque H3 introduit un vrai bloc.
+- Veiller à ne pas ajouter de H3 « décoratifs » : réserver H3 aux sous-sections réelles.
+- Progression logique H1 > H2 > H3 respectée (pas de saut de niveau).
+
+## Statut : H1 déployé dans `static/site.html` ; hiérarchie validée.
diff --git a/content/meta_homepage.md b/content/meta_homepage.md
new file mode 100644
index 0000000..eb5a56d
--- /dev/null
+++ b/content/meta_homepage.md
@@ -0,0 +1,20 @@
+# Meta — homepage maltai.fr
+
+## Title (inchangé, conforme)
+`MaltaiAI — Workspace IA open source et auto-hébergé`  (51 caractères)
+
+## Meta description (réécrite)
+**Avant (245 car., trop longue) :**
+> MaltaiAI est un workspace IA open source et auto-hébergé : chat multi-providers (OpenAI, Ollama, OpenRouter), agents outillés, navigation web, gestion de fichiers, mémoire vectorielle et console d'administration. Déployable en Docker ou Coolify.
+
+**Après (153 car.) :**
+> MaltaiAI : workspace IA open source et auto-hébergé. Chat multi-providers, agents outillés, mémoire, fichiers et administration dans une seule interface.
+
+## Mots-clés conservés
+`workspace IA`, `open source`, `auto-hébergé`, `agents`, `multi-providers`.
+
+## Open Graph / Twitter
+- `og:description` / `twitter:description` : version courte orientée fonctionnalités (déjà en place).
+- `og:image` / `twitter:image` : `https://maltai.fr/static/og-image.png` (1200×630).
+
+## Statut : déployé dans `static/site.html`.
diff --git a/docs/seo/anchors_review.md b/docs/seo/anchors_review.md
new file mode 100644
index 0000000..38bd6ba
--- /dev/null
+++ b/docs/seo/anchors_review.md
@@ -0,0 +1,25 @@
+# Revue des ancres internes — homepage
+
+## Constat (avant)
+Ancres répétées détectées :
+- `Connexion` ×3 (header, CTA final, footer)
+- `Ouvrir l'app` ×2
+- `GitHub` ×2
+
+## Analyse
+Ces répétitions sont **fonctionnelles** (navigation, conversion) et n'ont rien
+d'abusif. La priorité SEO est de **ne pas sur-optimiser** : on garde une UX claire.
+
+## Actions retenues
+- **Conserver** `Connexion` et `Ouvrir l'app` : libellés d'action attendus par
+  l'utilisateur, la diversité artificielle nuirait à l'UX.
+- **Diversifier les liens sortants GitHub** quand le contexte le permet :
+  - hero : `GitHub` → conserver (action courte)
+  - section Installer : `Voir le dépôt GitHub` ✔ (déjà différencié)
+  - footer : `GitHub` → peut devenir `Code source (GitHub)`.
+- Les liens de release sont déjà variés (`Télécharger le ZIP`, `Voir la release`,
+  `Télécharger Maltai`, `Voir le dépôt GitHub`).
+
+## Recommandation
+Aucune sur-optimisation. Une seule micro-amélioration possible (footer) ; impact
+SEO marginal, à faire seulement si cela n'alourdit pas l'UX.
diff --git a/docs/seo/backlinks_outreach_plan.md b/docs/seo/backlinks_outreach_plan.md
new file mode 100644
index 0000000..39f8183
--- /dev/null
+++ b/docs/seo/backlinks_outreach_plan.md
@@ -0,0 +1,25 @@
+# Plan d'acquisition de backlinks — maltai.fr
+
+**Constat :** très peu de domaines référents. Objectif : autorité externe.
+
+## Cibles prioritaires
+1. **Écosystème open source / self-hosted**
+   - awesome-selfhosted, awesome-llm, awesome-ai-agents (PR GitHub)
+   - selfh.st, r/selfhosted, r/LocalLLaMA
+2. **Communautés IA / dev**
+   - Hacker News (Show HN), Lobsters, dev.to, Hashnode
+   - Discord/forums Ollama, OpenRouter
+3. **Écosystème déploiement**
+   - Showcase / templates Coolify, communauté Docker
+4. **Annuaires produits**
+   - AlternativeTo (alternative à ChatGPT/LibreChat), Product Hunt, SaaSHub
+
+## Contenus de référence à publier (link bait)
+- Guide « Héberger son workspace IA avec Ollama + Coolify »
+- Comparatif MaltaiAI vs LibreChat vs OpenWebUI
+- Cas d'usage : agent web + mémoire + terminal admin
+
+## Suivi
+- KPI : nombre de domaines référents, liens dofollow obtenus, trafic referral.
+- Cadence : 2–3 actions outreach / semaine.
+- Outils : Search Console (liens), Ahrefs/Ubersuggest (gratuit) pour le suivi.
diff --git a/docs/seo/checklist_controle_mensuel.md b/docs/seo/checklist_controle_mensuel.md
new file mode 100644
index 0000000..1024a9f
--- /dev/null
+++ b/docs/seo/checklist_controle_mensuel.md
@@ -0,0 +1,23 @@
+# Checklist de contrôle SEO mensuel — maltai.fr
+
+## Technique
+- [ ] `https://maltai.fr/` répond en 200 ; variantes `www`/http en 301.
+- [ ] `canonical` = `https://maltai.fr/` ; `sitemap.xml` à jour (`lastmod`).
+- [ ] `robots.txt` cohérent (pages privées en Disallow).
+- [ ] Aucune image sans `alt` ; Lighthouse Perf/SEO/A11y ≥ 90.
+
+## Contenu
+- [ ] H1 unique et descriptif ; hiérarchie H2/H3 logique.
+- [ ] Meta description ≤ 160 caractères, orientée bénéfice.
+- [ ] Données structurées valides (Rich Results Test).
+
+## Search Console
+- [ ] Couverture / indexation sans erreur nouvelle.
+- [ ] CTR homepage (marque + génériques) suivi mois/mois.
+- [ ] Inspection d'URL après chaque mise en prod.
+
+## Autorité
+- [ ] Nombre de domaines référents (tendance).
+- [ ] Au moins 2 actions backlinks réalisées dans le mois.
+
+> Reporter les valeurs dans un tableau daté pour suivre l'évolution.
diff --git a/docs/seo/images_alt_inventory.md b/docs/seo/images_alt_inventory.md
new file mode 100644
index 0000000..8c76397
--- /dev/null
+++ b/docs/seo/images_alt_inventory.md
@@ -0,0 +1,18 @@
+# Inventaire des attributs `alt` — homepage
+
+| Fichier | Emplacement | Contexte | alt appliqué |
+|---|---|---|---|
+| `/static/logo-emblem.png` | header (lien marque) | logo cliquable | `Logo MaltaiAI` |
+| `/static/logo-full.png` | hero (arrière-plan) | filigrane décoratif (parent `aria-hidden`) | `MaltaiAI — workspace IA open source et auto-hébergé` |
+| `/static/og-image.png` | meta Open Graph | image de partage social | n/a (méta, pas de balise `<img>`) |
+
+## Règles retenues
+- Toute balise `<img>` visible porte un `alt` non vide.
+- Le filigrane du hero reste dans un conteneur `aria-hidden="true"` : l'`alt` sert
+  au crawler SEO sans pollution pour les lecteurs d'écran.
+- Largeur/hauteur (`width`/`height`) renseignées pour limiter le CLS.
+
+## Vérification
+- Inspecter le HTML : `grep -n "<img" static/site.html` → aucun `alt=""`.
+- Test accessibilité (axe / Lighthouse) : 0 image sans texte alternatif.
+- Re-crawl SEO : section « images sans alt » à 0.
diff --git a/docs/seo/redirections_301.md b/docs/seo/redirections_301.md
new file mode 100644
index 0000000..3397156
--- /dev/null
+++ b/docs/seo/redirections_301.md
@@ -0,0 +1,60 @@
+# Redirection 301 canonique — maltai.fr
+
+**Objectif :** éviter le duplicate content entre `www.maltai.fr` et `maltai.fr`.
+**URL canonique officielle :** `https://maltai.fr/`
+
+## Règles à appliquer
+| Source | Destination | Code |
+|---|---|---|
+| `http://maltai.fr/*` | `https://maltai.fr/*` | 301 |
+| `http://www.maltai.fr/*` | `https://maltai.fr/*` | 301 |
+| `https://www.maltai.fr/*` | `https://maltai.fr/*` | 301 |
+
+## Mise en œuvre
+
+### 1. Niveau application (déjà en place)
+Un middleware dans `app.py` redirige tout hôte `www.*` vers la version sans `www`
+en 301, chemin et query préservés :
+
+```python
+@app.middleware("http")
+async def canonical_host_redirect(request, call_next):
+    host = request.headers.get("host", "")
+    if host.startswith("www."):
+        target = f"https://{host[4:]}{request.url.path}"
+        if request.url.query:
+            target += "?" + request.url.query
+        return RedirectResponse(target, status_code=301)
+    return await call_next(request)
+```
+
+> Le passage `http → https` est géré par le proxy/Coolify (TLS terminé en amont).
+
+### 2. Niveau proxy (recommandé en complément — Coolify / Traefik)
+Idéalement, traiter la redirection avant l'app. Avec Traefik (Coolify) :
+
+```yaml
+# Labels du service
+- "traefik.http.routers.maltai-www.rule=Host(`www.maltai.fr`)"
+- "traefik.http.routers.maltai-www.middlewares=maltai-redir-nonwww"
+- "traefik.http.middlewares.maltai-redir-nonwww.redirectregex.regex=^https?://www\\.maltai\\.fr/(.*)"
+- "traefik.http.middlewares.maltai-redir-nonwww.redirectregex.replacement=https://maltai.fr/$${1}"
+- "traefik.http.middlewares.maltai-redir-nonwww.redirectregex.permanent=true"
+```
+
+Si `www` n'est pas censé exister, ne pas créer d'enregistrement DNS/cert `www`
+suffit aussi à supprimer le risque.
+
+## Cohérence à vérifier
+- Balise `<link rel="canonical" href="https://maltai.fr/">` ✔ (déjà présente)
+- `sitemap.xml` ne contient que des URL canoniques `https://maltai.fr/` ✔
+- Liens internes en chemins relatifs (`/app`, `/login`) ✔
+- Google Search Console : déclarer la propriété `https://maltai.fr/`
+
+## Vérification
+```bash
+curl -sI https://www.maltai.fr/        | grep -i "^HTTP\|^location"   # attendu : 301 -> https://maltai.fr/
+curl -sI http://maltai.fr/             | grep -i "^HTTP\|^location"   # attendu : 301/308 -> https
+curl -sI https://maltai.fr/            | grep -i "^HTTP"              # attendu : 200
+```
+**Sortie attendue :** chaque variante non canonique répond en 301 vers `https://maltai.fr/...`.
diff --git a/docs/seo/schema_markup_plan.md b/docs/seo/schema_markup_plan.md
new file mode 100644
index 0000000..85c54e9
--- /dev/null
+++ b/docs/seo/schema_markup_plan.md
@@ -0,0 +1,18 @@
+# Plan de données structurées (schema.org) — maltai.fr
+
+## En place (homepage)
+- **SoftwareApplication** : nom, catégorie, OS, description, version, licence,
+  dépôt, prix (0 EUR). ✔
+- **FAQPage** : 6 questions/réponses alignées sur la section FAQ visible. ✔
+
+## Validation
+- Test des résultats enrichis Google : https://search.google.com/test/rich-results
+- Validator schema.org : https://validator.schema.org/
+- Attendu : `SoftwareApplication` et `FAQPage` détectés sans erreur.
+
+## Évolutions possibles (si pertinent)
+- **Organization** (logo, sameAs vers GitHub) pour le knowledge panel.
+- **BreadcrumbList** si des pages secondaires sont ajoutées.
+- **WebSite** + `potentialAction` SearchAction si une recherche publique existe.
+
+> Ne pas ajouter de markup non visible/contredisant le contenu (risque de pénalité).
diff --git a/exports/plan_execution_maltai_seo.json b/exports/plan_execution_maltai_seo.json
new file mode 100644
index 0000000..70174f1
--- /dev/null
+++ b/exports/plan_execution_maltai_seo.json
@@ -0,0 +1,151 @@
+{
+  "site": "https://maltai.fr/",
+  "date": "2026-06-28",
+  "priorites": {
+    "P0": [
+      "Redirection 301 www -> non-www",
+      "Réécriture meta description",
+      "Réécriture H1"
+    ],
+    "P1": [
+      "Attributs alt sur les images",
+      "Hiérarchie H2/H3",
+      "Revue des ancres internes"
+    ],
+    "P2": [
+      "Données structurées (déjà en place)",
+      "Campagne backlinks",
+      "Checklist SEO mensuelle"
+    ]
+  },
+  "taches": [
+    {
+      "id": "P0-1",
+      "titre": "Redirection 301 canonique www/non-www",
+      "statut": "fait",
+      "fichier": "app.py + docs/seo/redirections_301.md"
+    },
+    {
+      "id": "P0-2",
+      "titre": "Meta description (245 -> 153 caractères)",
+      "statut": "fait",
+      "fichier": "static/site.html, content/meta_homepage.md"
+    },
+    {
+      "id": "P0-3",
+      "titre": "H1 descriptif unique",
+      "statut": "fait",
+      "fichier": "static/site.html, static/site.css, content/homepage_headings.md"
+    },
+    {
+      "id": "P1-1",
+      "titre": "Attributs alt sur logos",
+      "statut": "fait",
+      "fichier": "static/site.html, docs/seo/images_alt_inventory.md"
+    },
+    {
+      "id": "P1-2",
+      "titre": "Hiérarchie des titres H2/H3",
+      "statut": "validé",
+      "fichier": "content/homepage_headings.md"
+    },
+    {
+      "id": "P1-3",
+      "titre": "Revue des ancres internes",
+      "statut": "documenté",
+      "fichier": "docs/seo/anchors_review.md"
+    },
+    {
+      "id": "P2-1",
+      "titre": "Données structurées schema.org",
+      "statut": "en place",
+      "fichier": "static/site.html, docs/seo/schema_markup_plan.md"
+    },
+    {
+      "id": "P2-2",
+      "titre": "Plan backlinks",
+      "statut": "planifié",
+      "fichier": "docs/seo/backlinks_outreach_plan.md"
+    },
+    {
+      "id": "P2-3",
+      "titre": "Suivi SEO mensuel",
+      "statut": "planifié",
+      "fichier": "docs/seo/checklist_controle_mensuel.md"
+    }
+  ],
+  "responsables": {
+    "Dev/Ops": [
+      "P0-1",
+      "P0-3",
+      "P1-1",
+      "P2-1"
+    ],
+    "SEO/Content": [
+      "P0-2",
+      "P0-3",
+      "P1-2",
+      "P1-3"
+    ],
+    "Marketing/Founder": [
+      "P2-2"
+    ],
+    "SEO/Ops": [
+      "P2-3"
+    ]
+  },
+  "livrables": [
+    "docs/seo/redirections_301.md",
+    "docs/seo/images_alt_inventory.md",
+    "docs/seo/anchors_review.md",
+    "docs/seo/schema_markup_plan.md",
+    "docs/seo/backlinks_outreach_plan.md",
+    "docs/seo/checklist_controle_mensuel.md",
+    "content/meta_homepage.md",
+    "content/homepage_headings.md",
+    "exports/plan_execution_maltai_seo.json"
+  ],
+  "verifications": {
+    "techniques": [
+      "Variante non canonique -> 301 vers https://maltai.fr/",
+      "canonical = https://maltai.fr/",
+      "sitemap = URL canoniques uniquement",
+      "toutes les images ont un alt"
+    ],
+    "editoriales": [
+      "H1 unique, descriptif (>20 caractères), cohérent avec le title",
+      "meta description <= 160 caractères, orientée bénéfice",
+      "hiérarchie de titres logique H1>H2>H3"
+    ],
+    "suivi": [
+      "nouveau crawl SEO après mise en production",
+      "Search Console: indexation, couverture, inspection d'URL",
+      "suivi CTR homepage et domaines référents"
+    ]
+  },
+  "planning": {
+    "semaine_1": [
+      "Redirection 301",
+      "Meta description",
+      "H1 homepage",
+      "Alt images"
+    ],
+    "semaine_2": [
+      "Hiérarchie H2/H3",
+      "Revue ancres",
+      "Vérif Search Console / sitemap / canonical"
+    ],
+    "semaine_3_4": [
+      "Plan backlinks",
+      "Schema markup",
+      "Re-audit SEO avant/après"
+    ]
+  },
+  "criteres_succes": [
+    "problèmes critiques du rapport fermés",
+    "H1 et meta description optimisés",
+    "images avec alt",
+    "canonicalisation d'URL propre",
+    "amélioration des sections Server / Meta information / Page structure au re-crawl"
+  ]
+}
\ No newline at end of file
diff --git a/static/site.css b/static/site.css
index c876733..ab2b1cb 100644
--- a/static/site.css
+++ b/static/site.css
@@ -938,3 +938,19 @@ h1 {
   .security-grid div:hover, .workflow-list div:hover,
   .plan-list div:hover, .metrics div:hover { transform: none; }
 }
+
+
+/* H1 SEO : marque imposante + sous-titre descriptif lisible */
+h1 .h1-brand { display: block; }
+h1 .h1-sub {
+  display: block;
+  margin-top: 16px;
+  max-width: 720px;
+  font-size: clamp(20px, 2.4vw, 30px);
+  line-height: 1.18;
+  font-weight: 800;
+  letter-spacing: 0;
+  background: none;
+  color: #d8e1ea;
+  -webkit-text-fill-color: #d8e1ea;
+}
diff --git a/static/site.html b/static/site.html
index 9b04021..875be1b 100644
--- a/static/site.html
+++ b/static/site.html
@@ -5,7 +5,7 @@
   <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
   <meta name="theme-color" content="#0b0d14" />
   <meta name="robots" content="index, follow" />
-  <meta name="description" content="MaltaiAI est un workspace IA open source et auto-hébergé : chat multi-providers (OpenAI, Ollama, OpenRouter), agents outillés, navigation web, gestion de fichiers, mémoire vectorielle et console d'administration. Déployable en Docker ou Coolify." />
+  <meta name="description" content="MaltaiAI : workspace IA open source et auto-hébergé. Chat multi-providers, agents outillés, mémoire, fichiers et administration dans une seule interface." />
   <meta name="author" content="MaltaiAI" />
   <link rel="canonical" href="https://maltai.fr/" />
 
@@ -57,7 +57,7 @@
 
   <header class="site-topbar">
     <a class="site-brand" href="/" aria-label="Accueil MaltaiAI">
-      <img src="/static/logo-emblem.png" alt="" width="34" height="34" />
+      <img src="/static/logo-emblem.png" alt="Logo MaltaiAI" width="34" height="34" />
       <span>Maltai<span>AI</span></span>
     </a>
     <nav class="site-nav" aria-label="Navigation principale">
@@ -79,11 +79,11 @@
   <main id="main">
     <section class="hero">
       <div class="hero-backdrop" aria-hidden="true">
-        <img src="/static/logo-full.png" alt="" />
+        <img src="/static/logo-full.png" alt="MaltaiAI — workspace IA open source et auto-hébergé" />
       </div>
       <div class="hero-content reveal">
         <p class="eyebrow">Open source · auto-hébergé · mobile</p>
-        <h1>MaltaiAI</h1>
+        <h1><span class="h1-brand">MaltaiAI</span><span class="h1-sub">Le workspace IA open source et auto-hébergé pour agents, fichiers et automatisation</span></h1>
         <p class="hero-copy">
           Le workspace IA pour centraliser tes providers, lancer des agents outillés,
           gérer Ollama, explorer le web, manipuler des fichiers et garder le contrôle sur ton instance.
